#include "main.h"

/**
 * _islower - This program prints lowercase character
 * @c: The character to be printed
 * Return: 1 if c is lowercase, else 0
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
